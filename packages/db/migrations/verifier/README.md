# verifier migrations

Phase 1 creates only the migration runner and role/schema bootstrap. Product SQL migrations start in Phase 3.
Number migrations as `0001-description.sql`; applied SQL is immutable and checksum-verified.
